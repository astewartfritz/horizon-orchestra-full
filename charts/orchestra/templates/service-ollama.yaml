{{- if .Values.ollama.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "orchestra.fullname" . }}-ollama
  labels:
    {{- include "orchestra.labels" . | nindent 4 }}
    app.kubernetes.io/component: ollama
spec:
  type: ClusterIP
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
      name: http
  selector:
    {{- include "orchestra.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ollama
{{- end }}
